# WC-Draw-Simulator
A WC (World Cup) draw and games simulator. The user makes the draw and the rest are handled by the app. Multiple platoforms/ datasers are used to extract info for the model deciding the results (logistic regression). 
